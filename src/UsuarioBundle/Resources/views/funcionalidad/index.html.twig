{% extends 'base.html.twig' %}

{% set __title = 'Listado de funcionalidades' %}

{% block content %}
    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-body table-responsive no-padding">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Id</th>
                                <th>Nombre</th>
                                <th>Descripcion</th>
                                <th>Activo</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for funcionalidad in funcionalidads %}
                            <tr>
                                <td><a href="{{ path('funcionalidad_show', { 'id': funcionalidad.id }) }}">{{ funcionalidad.id }}</a></td>
                                <td>{{ funcionalidad.nombre }}</td>
                                <td>{{ funcionalidad.descripcion }}</td>
                                <td>{% if funcionalidad.activo %}<span class="label label-success">Si</span{% else %}<span class="label label-danger">No</span>{% endif %}</td>                                <td>
                                    <a title="Editar"   class="btn btn-default btn-sm" href="{{ path('funcionalidad_edit', { 'id': funcionalidad.id }) }}"><i class="glyphicon glyphicon-pencil"></i></a>
                                    <a title="Eliminar" class="btn btn-default btn-sm delete-obj" data-id="{{ funcionalidad.id }}" data-href="{{ path('funcionalidad_delete', { 'id': funcionalidad.id }) }}"> <i class="glyphicon glyphicon-remove-circle"></i></a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div><!-- /.box-body -->
            </div><!-- /.box -->
            {{ knp_pagination_render(funcionalidads) }}

        </div>
    </div>

    <div class="delete-obj-form hidden">
        {{ form_start(delete_form) }}
        <input type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </div>

    <a class="btn btn-primary" href="{{ path('funcionalidad_new') }}">Nueva funcionalidad</a>

{% endblock %}

