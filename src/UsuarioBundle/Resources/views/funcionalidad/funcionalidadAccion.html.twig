{% set accion_prototype %}
    <tr>
        <td>{{ form_widget(form.funcionalidadesAccion.vars.prototype.accion, { 'attr': {'class': 'select2'} }) }}</td>
        <td class="hidden">{{ form_widget(form.funcionalidadesAccion.vars.prototype.funcionalidad) }}</td>
        <td>{{ form_widget(form.funcionalidadesAccion.vars.prototype.activo) }}</td>
        <td class="text-center vert-align" ><a class="btn btn-danger btn-xs remove-accion">Borrar</a></td>
    </tr>
{% endset %}

<div class="box">
    <div class="box-body no-padding">
        <table class="table table-hover funcionalidades">
            <tbody data-prototype="{{ accion_prototype |e('html_attr') }}">
            <tr>
                <th>Acción</th>
                <th class="hidden">Funcionalidad</th>
                <th>Activo</th>
                <th>Acciones</th>
            </tr>
            {% for funcionalidadAccion in form.funcionalidadesAccion %}
                <tr>
                    <td>{{ form_widget(funcionalidadAccion.accion, { 'attr': {'class': 'select2'} }) }}
                        {{ form_errors(funcionalidadAccion.accion, { 'attr': {'class': 'error-item'} }) }}
                    </td>
                    <td class="hidden">{{ form_widget(funcionalidadAccion.funcionalidad) }}</td>
                    <td>{{ form_widget(funcionalidadAccion.activo) }}</td>
                    <td class="text-center vert-align"><a class="btn btn-danger btn-xs remove-accion">Borrar</a></td>
                </tr>

            {% endfor %}
            </tbody>
        </table>
    </div><!-- /.box-body -->
    <div class="box-footer clearfix">
        <div class="pull-right no-margin">
            <button id="agregar-accion" type="button" class="btn btn-success "><i class="fa fa-plus"></i>Agregar acción</button>
        </div>

    </div>
</div>